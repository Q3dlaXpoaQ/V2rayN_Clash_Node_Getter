name: Run Python Script

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: pip install requests

    - name: Run Python script
      run: python get.py

    - name: Commit changes
      run: |
        git config --global user.email "C_wz__@outlook.com"
        git config --global user.name "Q3dlaXpoaQ"
        git add .
        git commit -m "Update files"
        git push
